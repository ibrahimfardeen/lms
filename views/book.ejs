<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Library Management System - Loading Animation</title>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f2f2f2;
    }

    .loader {
      position: relative;
      width: 80px;
      height: 80px;
    }

    .book {
      width: 40px;
      height: 60px;
      background-color: #fff;
      position: absolute;
      top: 10px;
      left: 20px;
      transform-origin: bottom;
      transform-style: preserve-3d;
      animation: book-spin 2s infinite linear;
    }

    .page {
      width: 40px;
      height: 5px;
      background-color: #e74c3c;
      position: absolute;
      top: 50%;
      transform: translate(-50%, -50%);
      animation: page-flip 2s infinite ease-in-out;
    }

    @keyframes book-spin {
      0% {
        transform: rotateY(0);
      }
      100% {
        transform: rotateY(1turn);
      }
    }

    @keyframes page-flip {
      0% {
        transform: translate(-50%, -50%) rotateY(0);
      }
      50% {
        transform: translate(-50%, -50%) rotateY(90deg);
      }
      100% {
        transform: translate(-50%, -50%) rotateY(180deg);
      }
    }
  </style>
</head>
<body>
  <div class="loader">
    <div class="book">
      <div class="page"></div>
    </div>
  </div>
</body>
</html>
